In this stage, you'll implement support for printing error messages for
invalid commands.

Example:

#+begin_example
$ xyz
xyz: command not found
#+end_example

For now, we'll treat all commands as “invalid”. In later stages we'll
handle executing “valid” commands like =echo=, =cd= etc.
